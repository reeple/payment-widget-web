const routes = {
  auth: '/auth',
  home: '/',
  selectInstitution: {
    index: '/select-institution',
  },
  success: '/successful',
};

export const apiRoutes = {
  getCompany: '/company/key/%key%',
};

export default routes;
